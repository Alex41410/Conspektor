# AI Summarizer Pro - Инструкция по запуску

## 🚀 Быстрый старт (один клик!)

### Windows:
1. **Дважды кликните на файл `start.bat`**
2. Дождитесь автоматической установки зависимостей
3. Приложение откроется в браузере автоматически

### Альтернативный способ:
1. Откройте терминал в папке проекта
2. Выполните: `python start.py`

---

## 📋 Что делает скрипт запуска:

✅ Проверяет наличие Python и Node.js  
✅ Создает виртуальное окружение для backend  
✅ Устанавливает все зависимости автоматически  
✅ Проверяет и освобождает порты 8000 и 5173 при необходимости  
✅ Запускает backend сервер (порт 8000)  
✅ Запускает frontend сервер (порт 5173)  
✅ Открывает браузер с приложением  

---

## ⚙️ Требования:

- **Python 3.8+** - [Скачать](https://www.python.org/downloads/)
- **Node.js 16+** - [Скачать](https://nodejs.org/)
- **LM Studio** - [Скачать](https://lmstudio.ai/) (опционально, для генерации конспектов)

---

## 🔧 Настройка LM Studio:

1. Установите LM Studio
2. Загрузите модель (рекомендуется ~7B параметров для GTX 1650)
3. Откройте раздел "Local Server"
4. Нажмите "Start Server"
5. Убедитесь, что порт **1234** активен

---

## 📁 Структура проекта:

```
Конспектор/
├── start.py          # Главный скрипт запуска (Python)
├── start.bat         # Windows ярлык для запуска
├── backend/          # Backend сервер (FastAPI)
│   ├── main.py       # Основной файл API
│   ├── processor.py  # Обработка PDF
│   ├── lm_studio_client.py  # Клиент для LM Studio
│   └── config.json   # Конфигурация
├── frontend/         # Frontend приложение (React + Vite)
└── README_RU.md      # Эта инструкция
```

---

## 🐛 Решение проблем:

### Python не найден
- Установите Python с https://www.python.org/
- При установке отметьте "Add Python to PATH"
- Перезапустите терминал после установки

### Node.js не найден
- Установите Node.js с https://nodejs.org/
- Перезапустите терминал после установки

### npm не найден
- npm устанавливается вместе с Node.js
- Перезапустите терминал или перезагрузите компьютер
- Если не помогло - переустановите Node.js

### LM Studio не доступен
- Убедитесь, что LM Studio запущен
- Проверьте, что локальный сервер активен на порту 1234
- Приложение будет работать, но без генерации конспектов

### Порты заняты (8000 или 5173)
- Скрипт автоматически попытается освободить порты
- Если не получилось - закройте процессы вручную:
  ```cmd
  netstat -ano | findstr :8000
  taskkill /F /PID <номер_процесса>
  ```
- Или перезагрузите компьютер

### Backend не запускается
- Проверьте, что все зависимости установлены
- Запустите вручную для диагностики:
  ```cmd
  cd backend
  venv\Scripts\python.exe main.py
  ```

---

## 📝 Использование:

1. После запуска браузер откроется автоматически на http://localhost:5173
2. Перетащите PDF файл в зону загрузки
3. Дождитесь обработки (прогресс отображается в реальном времени)
4. Просмотрите конспект в окне Live Preview
5. Скачайте готовый конспект в формате .docx

---

## 🛑 Остановка:

Нажмите `Ctrl+C` в терминале, где запущен скрипт

---

## ⚙️ Конфигурация:

Настройки хранятся в `backend/config.json`:
- `output_dir` - директория для сохранения результатов
- `lm_studio_port` - порт LM Studio (по умолчанию 1234)
- `lm_studio_url` - URL LM Studio API
- `lm_studio_model` - имя модели в LM Studio
- `max_chunk_size` - максимальный размер чанка (15000 символов)
- `split_keywords` - ключевые слова для нарезки текста

Настройки можно изменить через панель настроек в интерфейсе приложения.

---

## 📞 Поддержка:

При возникновении проблем проверьте:
- Все зависимости установлены (Python, Node.js)
- LM Studio запущен и сервер активен (если нужна генерация)
- Порты 8000 и 5173 свободны
- Файрвол не блокирует соединения

---

## 🔄 Обновление:

Для обновления зависимостей:
1. Удалите папки `backend/venv` и `frontend/node_modules`
2. Запустите `start.bat` снова - зависимости установятся заново
