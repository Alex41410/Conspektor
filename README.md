# AI Summarizer Pro

MVP десктопного приложения для автоматизации конспектирования книг из PDF с использованием AI.

> 📖 **Быстрый старт:** См. [QUICKSTART.md](QUICKSTART.md)  
> 🔧 **Подробная настройка:** См. [SETUP.md](SETUP.md)  
> 🇷🇺 **Инструкция на русском:** См. [README_RU.md](README_RU.md)

## 🚀 Быстрый запуск (один файл!)

**Windows:** Дважды кликните `start.bat`  
**Linux/Mac:** Выполните `python start.py`

Скрипт автоматически проверит и установит все зависимости!

## Технологический стек

- **Backend**: Python (FastAPI)
- **Frontend**: React (Vite) + Tailwind CSS + Lucide Icons
- **Инструменты**: 
  - LM Studio (локальный LLM API на порту 1234)
  - Pandoc (конвертация в .docx)
  - pdfplumber (извлечение текста из PDF)

## Установка

### Автоматическая установка (рекомендуется)

1. Установите Python 3.8+ и Node.js 16+
2. Запустите `start.bat` (Windows) или `python start.py`
3. Скрипт автоматически установит все зависимости

### Ручная установка

**Backend:**

```bash
cd backend
python -m venv venv
venv\Scripts\activate  # Windows
# или source venv/bin/activate  # Linux/Mac
pip install -r requirements.txt
```

**Frontend:**

```bash
cd frontend
npm install
```

## Настройка LM Studio

1. Установите [LM Studio](https://lmstudio.ai/)
2. Загрузите модель (рекомендуется модель с ~7B параметрами для GTX 1650)
3. Запустите локальный сервер:
   - Откройте LM Studio
   - Перейдите в раздел "Local Server"
   - Нажмите "Start Server"
   - Убедитесь, что сервер работает на порту **1234**

## Использование

1. Запустите приложение через `start.bat` или `python start.py`
2. Откройте браузер и перейдите на `http://localhost:5173`
3. Перетащите PDF файл в зону загрузки
4. Дождитесь завершения обработки
5. Скачайте готовый конспект в формате .docx

## Конфигурация

Настройки хранятся в `backend/config.json`:
- `output_dir` - директория для сохранения результатов
- `lm_studio_port` - порт LM Studio (по умолчанию 1234)
- `lm_studio_url` - URL LM Studio API
- `lm_studio_model` - имя модели в LM Studio
- `max_chunk_size` - максимальный размер чанка для нейросети (15000 символов)
- `split_keywords` - ключевые слова для нарезки текста на главы

## Особенности

- ✅ Автоматическая проверка доступности сервисов при старте
- ✅ Умная нарезка текста на главы с использованием регулярных выражений
- ✅ Фильтрация мусорных фрагментов (оглавление, содержание)
- ✅ Очередь запросов к LM Studio для оптимизации использования VRAM
- ✅ Live Preview конспекта в реальном времени
- ✅ Конвертация в .docx через Pandoc
- ✅ Темная тема в стиле "Deep Sea"
- ✅ Автоматическая проверка и освобождение портов

## Структура проекта

```
.
├── backend/
│   ├── main.py           # FastAPI приложение
│   ├── processor.py      # Обработка PDF и нарезка текста
│   ├── lm_studio_client.py  # Клиент для LM Studio API
│   ├── config.json       # Конфигурация
│   └── requirements.txt  # Python зависимости
├── frontend/
│   ├── src/
│   │   ├── App.jsx       # Главный компонент
│   │   ├── components/   # React компоненты
│   │   └── index.css     # Стили
│   ├── package.json      # Node зависимости
│   └── vite.config.js    # Конфигурация Vite
├── start.py              # Главный скрипт запуска
├── start.bat             # Windows ярлык для запуска
└── README.md             # Этот файл
```

## Решение проблем

### Порты заняты (8000 или 5173)
- Скрипт автоматически попытается освободить порты
- Или закройте процессы вручную:
  ```cmd
  netstat -ano | findstr :8000
  taskkill /F /PID <номер_процесса>
  ```

### LM Studio не доступен
- Убедитесь, что LM Studio запущен и локальный сервер активен
- Приложение будет работать, но без генерации конспектов

### Python/Node.js не найден
- Установите зависимости и перезапустите терминал
- При установке Python отметьте "Add Python to PATH"

## Оптимизация для GTX 1650

Для оптимальной работы с GTX 1650 (4GB VRAM):

1. Используйте модель с ~7B параметрами
2. Установите `max_chunk_size: 15000` в config.json
3. Не обрабатывайте несколько PDF одновременно
4. Закройте другие приложения, использующие GPU

## Лицензия

MIT
